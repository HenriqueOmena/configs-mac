# Listar os plugins set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-resurrect'
bind-key R command-prompt "rename-session %%"
# Iniciar o TPM
run '~/.tmux/plugins/tpm/tpm'

set-option -g mouse on
